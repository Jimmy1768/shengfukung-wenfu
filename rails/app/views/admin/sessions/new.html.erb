<% oauth_links = oauth_provider_links %>

<section class="card login-card">
  <header>
    <p class="eyebrow">Temple admins</p>
    <h1>Temple Management System</h1>
    <p>Sign in to manage offerings, orders, and patron communications for your temple.</p>
  </header>

  <div class="login-grid single-temple">
    <div class="login-credentials field-panel register-panel register-social">
      <div class="login-panel-head">
        <h2 class="login-subhead">Continue with OAuth</h2>
        <p class="muted">Use the provider your owner or staff admin approved.</p>
      </div>
      <% if oauth_links.any? %>
        <div class="oauth-button-grid">
          <% oauth_links.each do |provider| %>
            <%= link_to "/auth/#{provider[:strategy]}", class: "btn btn-secondary oauth-btn" do %>
              <% if provider[:icon_path].present? %>
                <img src="<%= provider[:icon_path] %>" alt="" class="oauth-icon" />
              <% end %>
              <span><%= provider[:label] %></span>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <p class="muted">OAuth providers are not configured yet.</p>
      <% end %>
      <p class="muted">Need access? Ask your owner admin to invite you.</p>
    </div>

    <div class="login-actions field-panel register-panel register-email">
      <div class="login-panel-head">
        <h2 class="login-subhead">Sign in with email</h2>
        <p class="muted">Use the credentials from your onboarding email.</p>
      </div>
      <%= form_with url: admin_sessions_path, scope: :session, local: true, class: "login-form" do |f| %>
        <div class="form-field">
          <%= f.label :email, "Email" %>
          <%= f.email_field :email, required: true, autofocus: true, value: params.dig(:session, :email),
            placeholder: "admin@example.com" %>
        </div>
        <div class="form-field">
          <%= f.label :password, "Password" %>
          <%= f.password_field :password, required: true, placeholder: "••••••••" %>
        </div>
        <%= f.submit "Sign in", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>

  <footer class="login-footer">
    <p class="muted">
      Each admin account is scoped to a single temple. Owners can grant or revoke staff access anytime.
    </p>
  </footer>
</section>
