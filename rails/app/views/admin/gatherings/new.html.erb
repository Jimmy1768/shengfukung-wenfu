<div class="admin-stack">
  <div class="admin-stack__row">
    <section class="card stack-item stack-item--wide">
      <header class="admin-section-header">
        <div>
          <h1><%= t("admin.gatherings.new.title") %></h1>
          <p class="muted"><%= t("admin.gatherings.new.body") %></p>
        </div>
      </header>

      <%= render "form", gathering: @gathering %>
    </section>
  </div>
</div>
