<div class="admin-stack">
  <div class="admin-stack__row">
    <section class="card stack-item stack-item--xl stack-item--with-gutter admin-form-card">
      <header>
        <h1><%= t("admin.gallery_entries.new.title") %></h1>
        <p class="muted"><%= t("admin.gallery_entries.new.body") %></p>
      </header>

      <%= render "form", gallery_entry: @gallery_entry %>
    </section>
  </div>
</div>
