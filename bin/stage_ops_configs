#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
SCRIPT_PATH="$ROOT_DIR/ops/scripts/render_ops_templates.rb"

if [ ! -x "$SCRIPT_PATH" ]; then
  echo "render_ops_templates.rb not found at $SCRIPT_PATH" >&2
  exit 1
fi

"$SCRIPT_PATH" "$@"
